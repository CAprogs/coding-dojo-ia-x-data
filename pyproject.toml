[project]
name = "paris-event-analyzer"
version = "0.1.0"
description = "Data ingestion and storage for Paris events"
requires-python = ">=3.12"
dependencies = [
    "python-dotenv",
    "duckdb",
    "minio",
    "requests-cache",
    "rich",
]
